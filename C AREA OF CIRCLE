#include <stdio.h>
#define PI 3.14

int main() {
 
    printf("Enter the radius of circle: ");
    float radius;
    scanf("%f" , &radius);
    printf("Area of the circle is %f ",PI * radius * radius);

    return 0;
}
